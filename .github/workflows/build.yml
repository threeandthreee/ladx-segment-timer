name: Build timer

on:
  push:
    tags:
      - '**'

jobs:
  build-and-release:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            runner: ubuntu-latest
            exe-name: timer-linux
            shell: bash
          - os: windows-latest
            runner: windows-latest
            exe-name: timer-windows.exe
            shell: pwsh # Use PowerShell explicitly for Windows steps
          - os: macos-latest
            runner: macos-latest
            exe-name: timer-macos
            shell: bash

    runs-on: ${{ matrix.runner }}
    
    steps:
      - name: Checkout code with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Parse Version
        id: version
        shell: ${{ matrix.shell }}
        run: |
          # Get the tag name (e.g., v1.0.0)
          TAG="${{ github.ref_name }}"
          
          # Remove 'v' prefix if present (Bash style)
          if [[ "$TAG" == v* ]]; then
            VERSION="${TAG#v}"
          else
            VERSION="$TAG"
          fi
          
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Creating timer-version file..."
          echo "$VERSION" > timer-version

      - name: Prepare Directory Structure
        shell: ${{ matrix.shell }}
        run: |
          # Move pyevilemu to evilemu to match timer.spec
          if [ -d "pyevilemu" ]; then
            mv pyevilemu evilemu
          fi
          
          # Verify files exist for PyInstaller
          ls -la timer.py
          ls -la timer-version
          ls -la evilemu/ || echo "Warning: evilemu folder not found"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        shell: ${{ matrix.shell }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Executable
        shell: ${{ matrix.shell }}
        run: |
          pyinstaller timer.spec --clean

      - name: Rename Executable
        shell: ${{ matrix.shell }}
        run: |
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            mv dist/timer.exe ${{ matrix.exe-name }}
          else
            mv dist/timer ${{ matrix.exe-name }}
          fi

      - name: Create Release (if not exists)
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          name: Timer ${{ steps.version.outputs.version }}
          draft: true
          allowUpdates: true
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          omitPrereleaseDuringUpdate: true
          # This step requires "Write" permissions on the repo settings

      - name: Upload Artifact to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./${{ matrix.exe-name }}
          asset_name: ${{ matrix.exe-name }}
          asset_content_type: application/octet-stream